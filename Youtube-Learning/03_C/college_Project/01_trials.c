#include <stdio.h>
#include <conio.h>
int main()
{ int year;
  printf("Entera year \n");
  scanf("%d",&year);
  if
   {(year %4==0 ; year %100!==0)
    printf("Leap year \n");
    (year %400==0)
   { printf("%d is a leap year", year);
   }
  else
   { printf("%d is not leap year",year);
   }
     }
   return 0;
}